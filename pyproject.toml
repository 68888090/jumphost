[project]
name = "jumphost"
description = "跳板机管理工具 — 通过 frp 反向代理实现 SSH 跨网段访问"
license = "MIT"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/yulu/jumphost"
